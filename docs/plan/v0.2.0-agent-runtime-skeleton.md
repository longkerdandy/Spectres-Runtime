# Milestone Plan — v0.2.0

> Implement the structural skeleton of Spectres Runtime on top of Agno/AgentOS. This milestone adds the minimal code structure needed for agents, session/chat-history storage, and basic built-in tools. The project does not need to be fully runnable yet.
>
> Status: `draft`

---

## 1. Meta

| Field | Value |
|-------|-------|
| **Milestone** | `v0.2.0` |
| **Title** | Agent Runtime Skeleton |
| **Goal** | Establish a minimal but extensible Agno/AgentOS-based runtime structure with session management and a small set of built-in tools, without memory or knowledge-base features. |
| **Status** | `draft` |

---

## 2. Context

`v0.1.0` established the development environment, quality workflow, and GitHub security scanning. `v0.2.0` is the first application milestone. It focuses on **structure over behavior**: we want the right modules, interfaces, and configuration in place so that future milestones (memory, knowledge base, Client/Edge protocols) can plug in cleanly.

The design is inspired by `agentos-docker-template` but is intentionally **not a direct copy**. Spectres Runtime has its own architecture (Master/Slave agents, Client API, Edge Gateway Proxy) and will evolve separately.

Key constraints:

- Use Agno's `Agent`, `AgentOS`, and `Db` abstractions where they fit.
- Session and chat history must be persistent, but memory (user profiles, long-term recall) is explicitly out of scope.
- The database choice in this milestone must support future `mem0` or `hindsight` integration without a later migration.
- Code should be minimal: stubs, type annotations, and small tests are enough. The server does not need to start successfully in CI.

---

## 3. Scope

### 3.1 In Scope

1. **Agno dependency**
   - Add `agno` to `pyproject.toml`.
   - Update `uv.lock`.

2. **Project source structure**
   - Create `src/spectres/` subpackages:
     - `agents/` — Agent definitions (at least a Master Agent stub).
     - `tools/` — Built-in tool registrations.
     - `db/` — Database adapter/factory.
     - `config.py` — Typed settings via `pydantic-settings` (e.g. database URL, model API keys).
     - `main.py` — Minimal `AgentOS` entry point stub.
   - Keep each module small; empty `__init__.py` files and placeholder functions are acceptable.

3. **Database environment**
   - Add a local PostgreSQL service via `docker-compose.yml`.
   - Run the database in **Docker Desktop** for local development.
   - Provide an example environment file (`.env.example`) with database connection variables.
   - Add a database adapter/factory in `src/spectres/db/` that returns an Agno `PostgresDb` instance.
   - Document how to start the database in Docker Desktop for local development.

4. **Session / chat-history management**
   - Wire `PostgresDb` into the Master Agent stub so sessions can be persisted.
   - Expose a small `SessionManager` or `SessionService` abstraction (stub is fine) in `src/spectres/sessions/`.
   - Configure `add_history_to_context=True` and `num_history_runs` on the agent.

5. **Basic built-in tools**
   - Register **two** built-in Agno toolkits:
     - `CalculatorTools` — arithmetic helper.
     - `ShellTools` — run shell commands (e.g. `date`, `ls`).
   - Place tool wiring in `src/spectres/tools/builtin.py`.

6. **Tests**
   - Add minimal tests verifying that modules import cleanly and that configuration can be loaded.
   - Tests should not require a running database or model API keys.

7. **Documentation**
   - Update `README.md` with the new source layout and database startup instructions.

### 3.2 Out of Scope

- Long-term memory (`mem0`, `hindsight`, or Agno memory abstractions).
- Knowledge base / RAG / embeddings.
- Edge Gateway Proxy or device-control tools.
- Client API / AG-UI protocol endpoints.
- Authentication, authorization, multi-tenancy.
- Multi-agent teams, routing, or workflows.
- Docker image build for the runtime itself.
- Production deployment manifests.
- A fully runnable server in CI.

---

## 4. Success Criteria

- [ ] `agno` and required toolkit dependencies are declared in `pyproject.toml`.
- [ ] `src/spectres/` contains `agents/`, `tools/`, `db/`, `sessions/`, `config.py`, and `main.py`.
- [ ] `docker-compose.yml` can start a PostgreSQL instance locally.
- [ ] `.env.example` documents the required database and optional model-provider variables.
- [ ] A database adapter can construct an Agno `PostgresDb` from settings.
- [ ] The Master Agent stub is configured with `db`, `add_history_to_context`, and the two built-in tools.
- [ ] `uv run pytest` passes with new minimal tests.
- [ ] `uv run ruff check src tests` passes.
- [ ] `uv run ruff format --check src tests` passes.
- [ ] `uv run mypy src tests` passes.
- [ ] No secrets or generated artifacts are committed.

---

## 5. Database Selection

### 5.1 Requirements for v0.2.0

- Persistent storage for Agno **sessions** and **chat history**.
- Simple local development setup.
- Clean async/sync adapter interface for future milestones.

### 5.2 Requirements for Future Milestones

- Support for `mem0` or `hindsight` as the long-term memory backend.
- Potential support for vector storage when the knowledge base / RAG is added.
- Production-ready operation.

### 5.3 Local Development Environment

The local PostgreSQL instance will run inside **Docker Desktop** via `docker-compose.yml`. This keeps the development environment isolated from the host system and makes it easy to destroy and recreate the database during early iteration.

## 5.4 Options

| Option | Pros | Cons | Verdict |
|--------|------|------|---------|
| **SQLite** (`SqliteDb`) | Zero setup, single file, works offline. | Poor concurrency, no vector extension, likely requires migration to Postgres for memory/RAG later. | **Rejected** for the main storage backend. |
| **PostgreSQL** (`PostgresDb`) | Agno's recommended production DB; `pgvector` extension available for future RAG; works with managed services (Neon, Supabase, RDS); mem0/hindsight both support Postgres. | Requires running a Postgres service locally. | **Recommended**. |
| **MongoDB** (`MongoDb`) | Flexible schema, good for JSON-heavy session data. | Not the default in Agno docs; mem0/hindsight support less certain; adds a second database family to the stack. | **Rejected**. |
| **Redis** (`RedisDb`) | Fast, cache-friendly. | Not a durable primary store for sessions; not suitable for knowledge base. | **Rejected** for primary storage. |

### 5.4 Recommendation

Use **PostgreSQL with the `pgvector` extension** as the primary database from v0.2.0 onward.

Rationale:

1. **Future-proof**: `pgvector` is already the de-facto choice for Agno knowledge-base embeddings, and both `mem0` and `hindsight` document Postgres support.
2. **Single engine**: sessions, chat history, future memory, and future knowledge vectors can live in one database.
3. **Agno alignment**: `PostgresDb` is the default and most mature storage backend in Agno.
4. **Local parity with production**: managed Postgres services (Neon, Supabase, AWS RDS) are drop-in replacements for the local Docker container.

For v0.2.0, the runtime code only needs to construct `PostgresDb` from a `DATABASE_URL`; actual tables are managed by Agno's storage layer. The Docker Compose file will use the Agno-provided `agnohq/pgvector:18` image so the `pgvector` extension is available immediately and the local environment matches the official AgentOS template.

---

## 6. Task Breakdown

| ID | Task | Priority | Depends On | Acceptance Criteria |
|----|------|----------|------------|---------------------|
| 1 | Add `agno` dependency to `pyproject.toml` | High | - | `uv.lock` is regenerated and `uv sync --extra dev` succeeds. |
| 2 | Add `pydantic-settings` dependency for typed configuration | High | - | Dependency is present in `pyproject.toml` and lockfile. |
| 3 | Create `src/spectres/` package structure | High | 1-2 | Directories `agents/`, `tools/`, `db/`, `sessions/` exist with `__init__.py` files. |
| 4 | Implement `src/spectres/config.py` with database and model settings | High | 3 | Settings load from environment variables; `.env.example` is provided. |
| 5 | Add `docker-compose.yml` with PostgreSQL + pgvector for Docker Desktop | High | - | `docker compose up -d` starts Postgres on `localhost:5532` inside Docker Desktop. |
| 6 | Implement `src/spectres/db/postgres.py` adapter returning `PostgresDb` | High | 4-5 | Adapter constructs `PostgresDb` from settings without errors. |
| 7 | Create Master Agent stub in `src/spectres/agents/master.py` | High | 6 | Agent accepts `db`, `tools`, `add_history_to_context`, and `num_history_runs`. |
| 8 | Register `CalculatorTools` and `ShellTools` in `src/spectres/tools/builtin.py` | High | 3 | Tools are importable and can be passed to an Agent. |
| 9 | Create `src/spectres/sessions/session_manager.py` stub | Medium | 6 | Module defines a `SessionManager` class with a `get_db()` or similar method. |
| 10 | Implement minimal `src/spectres/main.py` AgentOS entry point stub | Medium | 7-9 | File imports `AgentOS`, `Master Agent`, and settings; no runtime required. |
| 11 | Add minimal import/load tests | High | 3-10 | `uv run pytest` passes without requiring a running DB or API keys. |
| 12 | Update `README.md` with source layout and Docker Desktop database startup | Medium | 5, 10 | README explains how to start Postgres in Docker Desktop and describes the package structure. |
| 13 | Run local quality checks and fix issues | High | 11-12 | ruff, mypy, and pytest all pass. |
| 14 | User review checkpoint | High | 13 | User approves the plan and the resulting skeleton before v0.3.0. |

---

## 7. Definition of Done

The milestone is complete when:

- [ ] All tasks in Section 6 are done.
- [ ] All success criteria in Section 4 are met.
- [ ] Quality checks pass locally and on GitHub Actions.
- [ ] The database choice in Section 5 is reviewed and approved by the user.
- [ ] Documentation is updated.
- [ ] User has reviewed and approved the result.
- [ ] No secrets or generated artifacts are committed.

---

## 8. References

- `AGENTS.md`
- `docs/plan/TEMPLATE.md`
- `docs/plan/v0.1.0-dev-env-quality-security.md`
- Agno AgentOS documentation: https://docs.agno.com/agent-os/overview
- Agno Storage / `PostgresDb`: https://docs.agno.com/features/storage
- Agno Chat History: https://docs.agno.com/database/chat-history
- Agno DuckDuckGo Tools: https://docs.agno.com/tools/toolkits/search/duckduckgo
- Agno Calculator Tools: https://docs.agno.com/tools/toolkits/others/calculator
- Agno pgvector Docker image: `agnohq/pgvector:18`
- mem0 documentation (future): https://docs.mem0.ai/
